version: '3'

services:
  node:
    container_name: ${PROJECT}_node
    build:
      context: .
      dockerfile: docker/node/Dockerfile.api
    working_dir: /home/node/app
    logging:
      options:
        max-size: '10m'
        max-file: '3'
    environment:
      HOST: 0.0.0.0
      PORT: 3005
    expose:
      - 3005
    volumes:
      - ./:/home/node/app
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=web"
      - "traefik.http.routers.${PROJECT}-backend.rule=Host(`${DOCKER_DOMAIN}`)"
      - "traefik.http.routers.${PROJECT}-backend.entrypoints=http"
      - "traefik.http.routers.${PROJECT}-backend.service=${PROJECT}-backend"
      - "traefik.http.services.${PROJECT}-backend.loadbalancer.server.port=3005"
      - "traefik.http.routers.${PROJECT}-backend-secure.rule=Host(`${DOCKER_DOMAIN}`)"
      - "traefik.http.routers.${PROJECT}-backend-secure.entrypoints=https"
      - "traefik.http.routers.${PROJECT}-backend-secure.tls=true"
      - "traefik.http.routers.${PROJECT}-backend-secure.tls.certresolver=acmeresolver"
      - "traefik.http.routers.${PROJECT}-backend-secure.service=${PROJECT}-backend"
    networks:
      - default
      - web
    command: sh -c 'yarn && yarn start'
    restart: ${RESTART}

  worker:
    container_name: ${PROJECT}_worker
    build:
      context: .
      dockerfile: docker/node/Dockerfile
    working_dir: /home/node/app
    logging:
      options:
        max-size: '10m'
        max-file: '3'
    environment:
      KATAGO_BIN: ${KATAGO_BIN:-/opt/katago/katago}
      KATAGO_CONFIG: ${KATAGO_CONFIG:-/home/node/app/katago/analysis.cfg}
      KATAGO_MODEL: ${KATAGO_MODEL:-/home/node/app/katago/models/default.bin.gz}
      KATAGO_MODEL_URL: ${KATAGO_MODEL_URL:-https://github.com/lightvector/KataGo/releases/download/v1.4.5/g170e-b20c256x2-s5303129600-d1228401921.bin.gz}
      KATAGO_QUERY_TIMEOUT_MS: ${KATAGO_QUERY_TIMEOUT_MS:-1800000}
      NVIDIA_VISIBLE_DEVICES: all
      NVIDIA_DRIVER_CAPABILITIES: compute,utility
    volumes:
      - ./:/home/node/app
      - ./docker/node/bootstrap.sh:/usr/local/bin/bootstrap.sh
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    networks:
      - default
    entrypoint: ['sh', '/usr/local/bin/bootstrap.sh']
    command: sh -c 'yarn && yarn worker'
    restart: ${RESTART}

  storage:
    container_name: ${PROJECT}_storage
    image: nginx:stable-alpine
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=web"
      - "traefik.http.routers.${PROJECT}-storage.rule=Host(`storage.${DOCKER_DOMAIN}`)"
      - "traefik.http.routers.${PROJECT}-storage.entrypoints=http"
      - "traefik.http.routers.${PROJECT}-storage.service=${PROJECT}-storage"
      - "traefik.http.services.${PROJECT}-storage.loadbalancer.server.port=80"
      - "traefik.http.routers.${PROJECT}-storage-secure.rule=Host(`storage.${DOCKER_DOMAIN}`)"
      - "traefik.http.routers.${PROJECT}-storage-secure.entrypoints=https"
      - "traefik.http.routers.${PROJECT}-storage-secure.tls=true"
      - "traefik.http.routers.${PROJECT}-storage-secure.tls.certresolver=acmeresolver"
      - "traefik.http.routers.${PROJECT}-storage-secure.service=${PROJECT}-storage"
    networks:
      - default
      - web
    volumes:
      - ./storage/kifu:/usr/share/nginx/html/kifu:ro
      - ./docker/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    restart: ${RESTART}

  db:
    container_name: ${PROJECT}_db
    image: postgres:12.2-alpine
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - 127.0.0.1:${DOCKER_DB_PORT}:5432
    volumes:
      - db_data:/var/lib/postgresql/data
    restart: ${RESTART}

volumes:
  db_data:

networks:
  web:
    external: true
