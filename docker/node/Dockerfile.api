FROM node:22.13.1-bookworm-slim

# API-only image: no KataGo / CUDA (worker uses docker/node/Dockerfile)

RUN apt-get update \
  && apt-get install -y --no-install-recommends \
    bash \
    ca-certificates \
    curl \
  && rm -rf /var/lib/apt/lists/*
