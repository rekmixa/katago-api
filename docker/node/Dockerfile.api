FROM node:22.13.1-bookworm-slim

# API-only image: no KataGo / GPU (worker uses docker/node/Dockerfile + TensorRT)

RUN apt-get update \
  && apt-get install -y --no-install-recommends \
    bash \
    ca-certificates \
    curl \
  && rm -rf /var/lib/apt/lists/*
